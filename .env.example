# Slack App credentials
# Get these from https://api.slack.com/apps after creating your app
SLACK_BOT_TOKEN=xoxb-your-bot-token-here
SLACK_APP_TOKEN=xapp-your-app-token-here
SLACK_SIGNING_SECRET=your-signing-secret-here

# Working directory Claude Code runs in
# Defaults to wherever you launch the bot from
# CLAUDE_WORK_DIR=/path/to/your/project

# Resume the most recent Claude Code session in the working directory on startup
# Useful if you want /new to pick up where you left off rather than starting fresh
# CLAUDE_CONTINUE=true

# Skip all permission prompts (file writes, bash commands, etc.)
# Claude will proceed without asking "do you want to create this file?" etc.
# Only enable this if you're comfortable with Claude operating autonomously
# CLAUDE_SKIP_PERMISSIONS=true
